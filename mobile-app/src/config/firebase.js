import auth from '@react-native-firebase/auth';

// The Firebase configuration is handled by google-services.json (Android) 
// and GoogleService-Info.plist (iOS) when using @react-native-firebase
// No manual config object is needed here.

export { auth };
